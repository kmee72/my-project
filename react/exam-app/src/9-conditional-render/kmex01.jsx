import React, { useState } from "react";

function 컴포넌트이름( props )
{
    조건문
    return JSX;
}



<자식컴포넌트이름 props이름={값} />

const 객체명 = {
   key: {
      css속성: 값
   }
}

const { 변수1, 변수2 } = props;

조건 ? 참일때 : 거짓일때

const [상태값, 상태변경함수] = useState(초기값);

